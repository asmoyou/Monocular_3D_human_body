# 快速启动指南

## 第一次使用

### 1. 安装依赖
```bash
# 激活conda环境
conda activate sam_3d_body

# 安装Python依赖（在项目根目录）
pip install -r requirements.txt

# 安装前端依赖
cd frontend
npm install
cd ..
```

### 2. 选择启动模式

根据你的显卡显存选择：

| 显存大小 | 推荐脚本 | 说明 |
|---------|---------|------|
| 12GB+ | `start.bat` | 完整功能，最高精度 |
| 6-11GB | `start-lightweight.bat` | **推荐**，禁用FOV估计 |
| <6GB | 需手动配置 | 见下文 |

### 3. 启动

```bash
# 轻量级模式（推荐）
start-lightweight.bat

# 或标准模式
start.bat
```

### 4. 访问
浏览器打开: `http://localhost:5173`

---

## 日常使用

### 启动
```bash
start-lightweight.bat
```

### 关闭
直接关闭两个命令行窗口即可

---

## 脚本说明

### start.bat
- **显存**: ~6-8GB
- **功能**: 完整功能
- **自动**: 激活conda环境 + 启动前后端

### start-lightweight.bat
- **显存**: ~4-5GB ⭐ **推荐**
- **功能**: 禁用FOV估计（影响很小）
- **自动**: 激活conda环境 + 启动前后端

### check-memory.bat
- **用途**: 检查显存使用情况
- **运行**: 需要几分钟
- **结果**: 显示不同模式的显存占用

---

## 故障排查

### 问题1: "Failed to activate conda environment"
**解决**:
```bash
# 检查环境是否存在
conda env list

# 如果不存在，创建环境
conda create -n sam_3d_body python=3.10
conda activate sam_3d_body
pip install -r requirements.txt
```

### 问题2: 显存不足 (OOM)
**解决**:
1. 使用 `start-lightweight.bat`
2. 关闭其他占用GPU的程序
3. 查看 `MEMORY_OPTIMIZATION.md` 了解更多优化选项

### 问题3: 模型未显示
**解决**:
1. 打开浏览器开发者工具 (F12)
2. 查看Console标签的错误信息
3. 检查是否有 `[Viewer]` 开头的日志
4. 尝试刷新页面

### 问题4: 无法控制3D视角
**解决**:
1. 确认模型已加载完成
2. 等待"Loading 3D model..."消失
3. 在3D画布上拖动鼠标：
   - 左键: 旋转
   - 右键: 平移
   - 滚轮: 缩放

---

## 手动启动（不使用脚本）

### 终端1: 后端
```bash
conda activate sam_3d_body
set LIGHTWEIGHT_MODE=true
python app.py
```

### 终端2: 前端
```bash
cd frontend
npm run dev
```

---

## 文件结构

```
sam-3d-body-demo/
├── start.bat                    # 标准模式启动
├── start-lightweight.bat        # 轻量级模式启动 ⭐
├── check-memory.bat             # 显存诊断
├── app.py                       # Flask后端
├── requirements.txt             # Python依赖
├── README.md                    # 完整文档
├── QUICK_START.md              # 本文件
├── MEMORY_OPTIMIZATION.md       # 显存优化指南
└── frontend/                    # React前端
    ├── package.json
    └── src/
```

---

## 常用命令

### 检查显存使用
```bash
check-memory.bat
```

### 查看conda环境
```bash
conda env list
```

### 更新依赖
```bash
# Python
conda activate sam_3d_body
pip install -r requirements.txt --upgrade

# 前端
cd frontend
npm update
```

---

## 需要帮助？

1. **完整文档**: 查看 `README.md`
2. **显存优化**: 查看 `MEMORY_OPTIMIZATION.md`
3. **问题修复**: 查看 `FIXES.md`
4. **控制台日志**: 打开浏览器F12查看错误

---

## 最佳实践

✅ **推荐**:
- 使用 `start-lightweight.bat`
- 上传清晰的人物照片
- 图片分辨率适中 (不超过2048px)
- 关闭其他GPU程序

❌ **避免**:
- 同时运行多个实例
- 上传超大图片
- 在GPU显存不足时使用标准模式

---

## 下一步

1. 运行 `start-lightweight.bat`
2. 访问 `http://localhost:5173`
3. 上传照片
4. 调整3D人体姿势
5. 享受！🎉
